import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.roomate',
  appName: 'roomate',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
